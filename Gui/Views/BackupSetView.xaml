<catel:UserControl x:Class="SKnoxConsulting.SafeAndSound.Gui.Views.BackupSetView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
				   xmlns:catel="http://catel.codeplex.com">
    
    <!-- Resources -->
    <UserControl.Resources>
    </UserControl.Resources>

    <!-- Content -->
    <catel:StackGrid Background="White" Margin="7">
        <catel:StackGrid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </catel:StackGrid.RowDefinitions>
        <catel:StackGrid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </catel:StackGrid.ColumnDefinitions>

        <Label Content="Backup Set Name" />
        <Label Content="{Binding Name}" />

        <Label Content="Source Directory" />
        <Label Content="{Binding SourceDirectory}" />

        <Label Content="Destination Directory" />
        <Label Content="{Binding DestinationDirectory}" />
    </catel:StackGrid>
</catel:UserControl>
