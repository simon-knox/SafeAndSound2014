<controls:SafeAndSoundDialog x:Class="SKnoxConsulting.SafeAndSound.Gui.Views.AboutDialog"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                  xmlns:mui="http://firstfloorsoftware.com/ModernUI"
                  xmlns:catel="http://catel.codeplex.com"
                  xmlns:controls="clr-namespace:SKnoxConsulting.SafeAndSound.Gui.Controls"
                  mc:Ignorable="d" 
                  d:DesignHeight="300" d:DesignWidth="300"
                  Title="About Safe and Sound">

    <!-- Note: ModernDialog buttons are defined in the constructor of the code behind class -->
    <catel:StackGrid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding ProductName}"/>
        <TextBlock Text="{Binding VersionNumber}"/>  
        <TextBlock Text="{Binding CopyrightInfo}"/>

    </catel:StackGrid>

</controls:SafeAndSoundDialog>
